{% extends 'base.html.twig' %}

{% block title %}{{ 'bazarr.title'|trans }}{% endblock %}

{% block stylesheets %}
{{ parent() }}
{% endblock %}

{% block body %}
<div class="page-header d-print-none">
  <div class="row align-items-center">
    <div class="col">
      <h2 class="page-title">{{ 'bazarr.title'|trans }}</h2>
      <div class="text-secondary">{{ 'bazarr.subtitle'|trans }}</div>
    </div>
    <div class="col-auto ms-auto">
      {% include 'bazarr/_nav.html.twig' %}
    </div>
  </div>
</div>

<div class="page-body">
  <div class="container-xl">

    {% if error %}
      {% include 'error/_service_banner.html.twig' with { service: 'Bazarr', wizard_route: 'admin_settings_index', configured_url: service_url, reason: 'unreachable' } %}
    {% else %}
      <div class="card">
        <div class="card-header">
          <h3 class="card-title">{{ 'bazarr.movies.title'|trans }}</h3>
        </div>
        {% if movies is empty %}
          <div class="card-body">
            <div class="empty">
              <p class="empty-title">{{ 'bazarr.movies.empty'|trans }}</p>
            </div>
          </div>
        {% else %}
          <div class="table-responsive">
            <table class="table table-vcenter card-table">
              <thead>
                <tr>
                  <th>{{ 'bazarr.movies.column_title'|trans }}</th>
                  <th>{{ 'bazarr.movies.column_year'|trans }}</th>
                  <th>{{ 'bazarr.movies.column_language'|trans }}</th>
                  <th>{{ 'bazarr.movies.column_missing'|trans }}</th>
                </tr>
              </thead>
              <tbody>
                {% for movie in movies %}
                  <tr>
                    <td>{{ movie.title|default('—') }}</td>
                    <td>{{ movie.year|default('—') }}</td>
                    <td>{{ movie.audio_language is iterable ? movie.audio_language|column('name')|join(', ') : '—' }}</td>
                    <td>
                      {% if movie.missing_subtitles is iterable and movie.missing_subtitles|length > 0 %}
                        <span class="badge bg-red-lt">{{ movie.missing_subtitles|length }}</span>
                      {% else %}
                        <span class="badge bg-green-lt">{{ 'bazarr.movies.none_missing'|trans }}</span>
                      {% endif %}
                    </td>
                  </tr>
                {% endfor %}
              </tbody>
            </table>
          </div>
        {% endif %}
      </div>
    {% endif %}

  </div>
</div>
{% endblock %}
